---
- hosts: XXXXXXXXXX
  sudo: yes

  tasks:
      - name:  test connection
        ping:      
      - name: Update system sources information
        apt:
           update_cache: yes
           mc
           htop
           build-essential
           aptitude
           upgrade: dist
           update_cache: yes           
        register: out
      - debug: var=out.stdout_lines 
